name: Pipeline de CI/CD - Meu Primeiro RobÃ´

# Quando este workflow deve ser executado?

on:

  push:

    branches: [ main ]

  pull_request:

    branches: [ main ]

# Definindo os trabalhos (jobs) que serÃ£o executados

jobs:

  # Job 1: Verificações basicas

  verificacoes-basicas:

    runs-on: ubuntu-latest

    steps:

    - name: 1. Baixando o coigo do repositorio

      uses: actions/checkout@v4      

    - name: 2. Verificando estrutura do projeto

      run: |

        echo "Listando arquivos do projeto:"

        ls -la

        echo ""

        echo "Verificando se arquivos essenciais existem:"

        if [ -f "app.js" ]; then

          echo "app.js encontrado"

        else

          echo "app.js nao encontrado"

          exit 1

        fi

    - name: 3. Configurando Node.js

      uses: actions/setup-node@v4

      with:

        node-version: '18'

    - name: 4. Executando a aplicacao

      run: |

        echo "Iniciando a aplicacao..."

        node app.js

        echo "Aplicacao executada com sucesso!"        

    - name: 5. Mensagem de sucesso

      run: |

        echo "Parabens! Seu primeiro pipeline de CI esta funcionando!"

        echo "Estatisticas da execucao:"

        echo "   - Reposiorio: ${{ github.repository }}"

        echo "   - Branch: ${{ github.ref_name }}"

        echo "   - Commit: ${{ github.sha }}"

        echo "   - Autor: ${{ github.actor }}"

